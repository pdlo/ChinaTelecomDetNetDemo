一个寄存A假如绑定了动作B和C，在if（condition）{ 可以通过编译，在我更新的代码中，可以看到，320行我注释掉了，可以顺利通过编译，615到619行部分，以及524到525部分，这俩部分可以看到按这种形似调用寄存器可以通过编译，现在的问题是时间戳是48位的，但是寄存器只能使用到32位
                                  use B
                            }else{
                                  use A
                            }    
  通过查阅实例，可以使用两个时间寄存器来存储，一个存储时间戳当中的前32位，一个存储时间戳当中的后16位，或者按照例子中的，使用一个pair来存储高位与低位的时间戳，但是目前我还没测试过这种时间戳的形式
