
1、header不能过长，否则出错
2、计算不能过于复杂，如：a=b+c+d或a=b+(bit<8>)c，一行包括两次运算，否则出错
3、寄存器动作中不能写if else等复杂语句，不支持乘除，仅支持加减与或非
4、寄存器动作的excute命令需写在当前大括号的最开头
5、寄存器数量、相关指令数量越多，编译时间越长
6、if else语句越复杂，编译时间越长
7、如果出错，编译结果需要等待完整的编译时间，故需要耐心
8、同一个寄存器或者寄存器动作建议不要在apply中调用多次，即使相互之间不会同时运行，但也会发生不可预测的错误